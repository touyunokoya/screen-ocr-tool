# 文字認識ツール 仕様書

バージョン: **v1.1.0** / 最終更新: 2026-07-08
公開先: https://github.com/touyunokoya/screen-ocr-tool

## 1. 概要

画面の任意の範囲をドラッグで指定し、その部分の文字(日本語・英語・数式)を認識して
`memo.md` に日時付きで自動追記し、同時にクリップボードへコピーする常駐型ツール。

- **完全無料・オフライン動作**(インターネット接続不要 ※初回のモデルダウンロードのみ使用済み)
- Windows 11 上で動作。タスクトレイに常駐(青い「字」アイコン)
- PC起動時に自動で立ち上がる(スタートアップ登録済み)

## 2. ファイル構成

| ファイル | 役割 |
|---|---|
| `ocr_tool.py` | 本体プログラム(設定もこのファイルの冒頭にある) |
| `start_ocr_tool.vbs` | 起動用ファイル(ダブルクリックで黒い画面を出さずに起動) |
| `memo.md` | 認識結果の保存先(自動作成・追記) |
| `使い方.md` | 利用者向けの説明書 |
| `仕様書.md` | この文書 |
| `ocr_tool.log` | 動作記録・エラー記録 |
| `C:\Tools\ocr_venv\` | ツール専用のPython環境(AIモデル含む、削除するとツールは動かない)。**OneDriveの容量を圧迫しないよう同期対象の外に配置**(2026-07-08移動) |
| `配布キット\` | 友人への配布・GitHub公開用の一式(セットアップスクリプト付き) |
| スタートアップ内 `文字認識ツール.lnk` | 自動起動用ショートカット(`Win+R`→`shell:startup` で確認可) |

## 3. 操作方法

| キー | モード | 速さ | 内容 |
|---|---|---|---|
| **Ctrl+Shift+X** | 文章 | 一瞬〜1秒 | 日本語+英語の普通の文章 |
| **Ctrl+Shift+Z** | 数式 | 1〜2秒 | 数式1つをLaTeX形式に変換 |
| **Ctrl+Shift+A** | 混在 | 1〜4秒 | 文章と数式が混ざった範囲(論文の段落など) |

共通の流れ: キーを押す → 画面が暗くなる → ドラッグで範囲選択 → 「ピッ」と音が鳴り
右下に結果プレビュー表示 → `memo.md` に追記+クリップボードにコピー。

- キャンセル: **Esc** または **右クリック**
- ショートカットキーは他のアプリには渡らない(ブラウザ等のショートカットと衝突しない)
- 複数モニタ対応(全画面にまたがって選択可能)
- 認識中に同じキーを押しても二重実行はされない

## 4. 認識エンジンの構成(自動選択)

3つのエンジンを内蔵し、モードと状況に応じて自動で使い分ける。

| エンジン | 種類 | 特徴 |
|---|---|---|
| Windows標準OCR(日本語) | OS内蔵 | 一瞬で認識。日本語はほぼ完璧。英語の r を「 と誤読する癖あり |
| Surya OCR 0.17(高精度モデル) | オフラインAI(GPU使用) | 日本語・英語・数式すべて高精度。GPUメモリを約2GB使用 |
| RapidLaTeXOCR(簡易数式モデル) | オフラインAI(CPU) | 数式専用の軽量エンジン。高精度モデル停止中の代替 |

### モードごとの動作

- **文章(X)**: Windows標準OCRで認識
  - 小さい画像は2〜3倍に拡大してから認識(精度向上)
  - 日本語の文字間に入る余計なスペースを自動除去
  - 英単語中の「→ r の誤認識を自動補正
  - 認識結果の70%超が英数字だった場合、高精度モデルで自動的に認識し直す(英語文書対策)
  - 日本語OCRが入っていないPC(英語版Windows等)では、英語エンジン→高精度モデルの順に自動フォールバック(v1.1.0)
- **数式(Z)**: 高精度モデルで認識(停止中・読み込み中は簡易数式モデルで代替)
- **混在(A)**: 高精度モデル専用。停止中は案内を表示して中断

### 高精度モデルの精度向上策(内部処理)

- 認識時に2倍拡大画像(highres)を併用(幅2000px以上の大きな選択範囲では拡大しない — メモリ急増防止、v1.1.0)
- 数式は `<math>` タグで返されるため、行全体の数式は `$$...$$`、文中の数式は `$...$` に変換
- HTML風の装飾タグ・特殊文字表記は自動除去/変換

## 5. 保存形式

`memo.md` への追記形式(UTF-8):

```markdown
## 2026-07-08 00:15(文章)        ← 日時+モード名(文章/数式/文章+数式)

認識されたテキスト

## 2026-07-08 00:16(数式)

$$
\hat{\beta} = (X^T X)^{-1} X^T y
$$
```

クリップボードには本文のみ(数式は `$$` なしのLaTeX)がコピーされる。

書き込みは排他制御されており(連続キャプチャでも行が混ざらない)、メモを他のアプリが
掴んでいて書けない場合は0.5秒間隔で3回まで自動リトライする(v1.1.0)。

## 6. GPUメモリ管理

高精度モデルはGPUメモリ(VRAM 8GB中 約2GB)を常時使用するため、次の自動管理を行う。

- **起動時**: バックグラウンドで高精度モデルを先読み(起動後15〜30秒で準備完了)
- **自動解放**: 20秒ごとにGPU全体の使用率を監視し、**85%を超えたら**モデルを自動停止して
  メモリを解放(通知表示)。認識処理の実行中は解放しない
- **停止中の動作**: 文章(X)=通常どおり、数式(Z)=簡易モデルで代替、混在(A)=案内表示
- **手動復帰**: トレイアイコン右クリック →「高精度モデルを再読み込み」
  (復帰後**30分間**は自動解放を停止し、ゲーム等と並行利用できる)
- **手動解放**: トレイメニュー「GPUメモリを解放(ゲーム前に)」

## 7. タスクトレイメニュー

| 項目 | 動作 |
|---|---|
| メモを開く | `memo.md` を既定のアプリで開く(アイコンの**ダブルクリック**でも開く) |
| フォルダを開く | ツールのフォルダを開く |
| 高精度モデルを再読み込み | 停止中のモデルを復帰(30分間 自動解放オフ) |
| GPUメモリを解放(ゲーム前に) | モデルを手動停止してVRAMを空ける |
| 終了 | ツールを完全終了 |

## 8. 設定(ocr_tool.py 冒頭で変更可能)

| 設定名 | 既定値 | 意味 |
|---|---|---|
| `HOTKEY_TEXT` | `ctrl+shift+x` | 文章モードのキー |
| `HOTKEY_MATH` | `ctrl+shift+z` | 数式モードのキー |
| `HOTKEY_MIXED` | `ctrl+shift+a` | 混在モードのキー |
| `MEMO_FILENAME` | `memo.md` | 保存先ファイル名 |
| `GPU_RELEASE_THRESHOLD` | `0.85` | 自動解放の使用率しきい値(85%) |
| `GPU_CHECK_INTERVAL_SEC` | `20` | 監視間隔(秒) |
| `RELOAD_PAUSE_MIN` | `30` | 手動復帰後に自動解放を止める時間(分) |

変更後はツールを終了 → `start_ocr_tool.vbs` で再起動すると反映される。

## 9. 内部の仕組み(技術情報)

- 言語/環境: Python 3.11.5(専用venv)、主要ライブラリ:
  surya-ocr **0.17.0**(※0.20以降はDocker必須構成のため0.17に固定)、
  torch **2.12.1**+cu126(GPU/CUDA、バージョン固定)、rapid_latex_ocr、winocr、keyboard、
  pystray、pyperclip、mss、Pillow
- ログは1MBを超えると自動で `.old` に退避(肥大化防止、v1.1.0)
- 起動処理: `onnxruntime` を最初にimport(DLL初期化競合の回避)。
  pythonw実行時のstdout/stderr無効化対策あり
- 範囲選択: 全仮想画面のスクリーンショットを撮ってから暗くして表示する方式
  (選択中に画面が動かない)。高DPI対応(SetProcessDpiAwareness)
- 二重起動防止: ポート50917のバインドで判定(本体起動時のみ。すでに起動中なら
  ダイアログを出して終了)
- GPU監視: `nvidia-smi` で使用量を取得(コンソール窓は出さない)
- AIモデルの保存場所: Suryaは `AppData\Local\datalab`、簡易数式モデルは venv 内
- `memo.md` はOneDrive配下にあるため、他のデバイスからも同期して閲覧可能

## 10. 更新履歴

| 版 | 日付 | 内容 |
|---|---|---|
| v1.0 | 2026-07-07 | 初版(3モード認識、GPU自動解放、配布キット、GitHub公開) |
| v1.1.0 | 2026-07-08 | デモGIF追加/日本語OCR無しPCへのフォールバック/メモ書き込みの排他+リトライ/巨大選択範囲の拡大抑制/ログローテーション/torch固定/トレイのダブルクリック対応 |

## 11. 既知の制限・注意点

- **管理者権限で動いているアプリ**(一部のゲームやシステム画面)が最前面のときは、
  ショートカットキーが反応しないことがある(Windowsの仕様)
- 数式認識は、手書き数式や特殊なフォントでは誤認識が増える(論文PDF等の標準的な
  数式フォントで最高精度)。結果は必ず目視確認を推奨
- 高精度モデルの読み込み中(起動後の数十秒)は、Z/Aモードは簡易動作または待機になる
- Windows標準OCRの言語は日本語エンジンのみ使用(英語文書は高精度モデルへ自動切替で対応)
- ツールの終了・再起動を繰り返すと、その都度モデル読み込み(15〜30秒)が発生する
