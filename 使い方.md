# 文字認識ツール 使い方

画面の一部を範囲選択して文字認識(OCR)し、`memo.md` に自動追記するツールです。
すべて**無料・オフライン**で動きます(インターネット接続は不要)。

## 起動方法

`start_ocr_tool.vbs` をダブルクリックすると起動します
(画面には何も出ませんが、右下のタスクトレイに青い「字」アイコンが常駐します)。
セットアップ時に「自動起動」を選んだ場合は、PCを起動すると自動で立ち上がります。

自動起動をやめたいとき: `Win + R` →「shell:startup」と入力してEnter →
開いたフォルダの「文字認識ツール」ショートカットを削除。

## 3つのモード

| キー | モード | 速さ | 用途 |
|---|---|---|---|
| **Ctrl + Shift + X** | 文章(日本語+英語) | 一瞬 | Webページ・PDF・字幕などの普通の文章 |
| **Ctrl + Shift + Z** | 数式のみ | 1〜2秒 | 数式1つをLaTeX形式にしたいとき |
| **Ctrl + Shift + A** | 文章+数式の混在 | 1〜4秒 | 論文の段落など、文章と数式が混ざった範囲 |

どのモードも高精度AIモデル(Surya)を優先して使います。英語だけの文章は
Xモードでも自動で高精度モデルに切り替わるので、モード選びに迷ったら
「文章はX、数式が入っていそうならA」で大丈夫です。

### 操作手順

1. 取り込みたい内容を画面に表示する
2. 上のキーを押す → 画面が少し暗くなる
3. 読み取りたい部分を**マウスでドラッグして囲む**
4. 「ピッ」と音が鳴り、右下に認識結果のプレビューが表示される

- やり直したいとき: **Esc** または **右クリック** でキャンセル
- 結果は `memo.md` に日時付きで追記され、**クリップボードにもコピー**されます(そのまま Ctrl+V で貼り付け可能)

### メモ

- 起動直後の20秒ほどは高精度モデルを読み込み中です。その間に使うと
  「読み込み中…」の通知が出るか、簡易エンジンで代替されます。
- 数式は `$$ ... $$`(LaTeX形式)、文中の数式は `$ ... $` でメモに記録されます。

## PDFを丸ごとテキスト化する(v1.3.0〜)

論文などのPDFを、数式をLaTeX形式にした状態でまるごとテキスト(Markdown)に変換できます。

1. トレイの「字」アイコンを右クリック →「**PDFを丸ごとテキスト化…**」
2. PDFファイルを選ぶ
3. 数分待つ(4ページで1分弱が目安)→ 完了通知とともに結果フォルダが開く
4. PDFと同じ場所にできる「(PDF名)_変換」フォルダの中の `.md` ファイルが結果です

- 数式は `$$ ... $$`(LaTeX)、図は画像ファイルとして抽出されます
- 変換もオフラインで行われ、PDFの内容が外部に送られることはありません
- 変換中も通常の範囲選択OCRはそのまま使えます

## GPUメモリの自動解放(ゲームをする人向け)

高精度モデルはGPUメモリを2GBほど使います。**ゲームなどでGPUメモリの使用率が85%を
超えると、モデルを自動で停止してメモリを空けます**(右下に通知が出ます)。

- 停止中も 文章(X) と 数式(Z) はそのまま使えます(数式は簡易エンジンで代替)
- 混在(A) を押すと「停止中」の案内が出ます
- **復帰方法**: トレイの「字」アイコンを右クリック →「**高精度モデルを再読み込み**」
  (復帰後30分間は自動解放しないので、ゲーム中でも意図的に使い続けられます)
- ゲームを始める前に手動で空けたいとき: 同メニューの「**GPUメモリを解放**」

しきい値などは `ocr_tool.py` 冒頭の `GPU_RELEASE_THRESHOLD` 等で変更できます。

## 終了方法

タスクトレイ(画面右下の ∧ マークの中)の青い「字」アイコンを右クリック → **終了**

## 設定を変えたいとき

`ocr_tool.py` をメモ帳で開くと、最初の方に設定欄があります:

- ショートカットキーを変える → `HOTKEY_TEXT` / `HOTKEY_MATH` / `HOTKEY_MIXED`
- 保存先ファイル名を変える → `MEMO_FILENAME`

変更後はツールを終了して `start_ocr_tool.vbs` で再起動してください。

## うまく動かないとき

- **memo.md が見つからない** → トレイの「字」アイコンを右クリック →「フォルダを開く」。
  そこが本当の保存場所です(フォルダを移動・二重に展開した場合、見ている場所と違うことがあります)。
  認識後に「メモに書き込めませんでした」と通知が出る場合は、Windowsの
  「ランサムウェアの防止」設定がブロックしている可能性があります(設定 → プライバシーとセキュリティ →
  Windowsセキュリティ → ウイルスと脅威の防止 → ランサムウェアの防止 で、このツールを許可)
- 認識されない・エラーが出る → 同じフォルダの `ocr_tool.log` にエラー記録が残ります
- ツールが反応しない → タスクトレイから終了して `start_ocr_tool.vbs` で再起動
- 「すでに起動しています」と出る → 正常です(二重起動を防いでいます)

## 仕組み(参考)

| 部分 | 使用技術 |
|---|---|
| 文章認識 | Windows標準OCR(日本語エンジン) |
| 数式認識 | RapidLaTeXOCR(無料のオフラインAI) |
| 混在認識 | Surya OCR(無料のオフラインAI、GPUで動作) |
